# Graph orchestration
langgraph>=0.6

# LangChain core + Anthropic integration
langchain-core>=0.3
langchain-anthropic>=0.3
# OpenAI-compatible endpoints (GitHub Models, Gemini): used for the cross-family comparison
langchain-openai>=0.3
anthropic>=0.68

# Retrieval (BM25 lessicale, nessun modello di embedding necessario)
rank_bm25==0.2.2

# Estrazione testo da PDF nel repository (docs/, report)
pypdf>=5.0

# Templating
Jinja2==3.1.6

# Configurazione
PyYAML>=6.0
